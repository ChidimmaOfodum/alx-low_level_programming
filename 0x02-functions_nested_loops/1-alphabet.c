#include "main.h"
/**
 * main - Entry point
 * Descrption: a program that calls a function
 * Return: void
 */

void print_alphabet(void)
{
	 {
                char i;

                for (i = 'a'; i <= 'z'; i++)
                {
                        putchar(i);
                }
                putchar('\n');
        }
}
