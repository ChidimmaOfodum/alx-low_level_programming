#include <stdio.h>

/**
  * main - prints the number of argument passed to it
  * @argc: argument count
  * @argv: argument vector
  * Return: 0 always
  */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	(void)argv;

	return (0);
}

