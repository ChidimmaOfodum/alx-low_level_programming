#include <stdio.h>

/**
 * main - Entry point
 * Description: printing single digits of base 10 integers
 * Return:0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10 ; i++)
	{
		printf("%d", i);
	}
	putchar('\n');

	return (0);
}
