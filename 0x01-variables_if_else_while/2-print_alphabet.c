#include <stdio.h>
/**
 * main - Entry point
 * Description :prints all lowercase letters of alphabets
 * Return: 0
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}

	return (0);
}
