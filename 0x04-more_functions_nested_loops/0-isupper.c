#include "main.h"

/**
 * _isupper: Entry point
 * @c: input parameter
 * Description: A function that checks if a letter is upper or lower
 * Return : 0 or 1
 */

int _isupper(int c)
{
	if (c >= 97 && c <= 90)
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
